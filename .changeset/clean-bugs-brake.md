---
"effect-view-server": patch
---

Harden the published package metadata by adding the root package export, preserving tree-shaking and engine metadata in the release artifact, and refreshing vulnerable transitive dependency ranges.
