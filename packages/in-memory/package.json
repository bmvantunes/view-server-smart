{
  "name": "@view-server/in-memory",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "vp run -t @view-server/runtime-core#build && vp pack",
    "test": "vp run -t @view-server/runtime-core#build && vp test run --coverage --typecheck"
  },
  "dependencies": {
    "@view-server/client": "workspace:*",
    "@view-server/config": "workspace:*",
    "@view-server/runtime-core": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@vitest/coverage-istanbul": "catalog:",
    "@vitest/runner": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
