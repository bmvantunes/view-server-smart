{
  "name": "@view-server/server",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "vp pack",
    "test": "vp run -t @view-server/runtime-core#build && vp test run --coverage --typecheck"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@view-server/client": "workspace:*",
    "@view-server/config": "workspace:*",
    "@view-server/protocol": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@types/node": "catalog:",
    "@view-server/runtime-core": "workspace:*",
    "@vitest/coverage-istanbul": "catalog:",
    "@vitest/runner": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
