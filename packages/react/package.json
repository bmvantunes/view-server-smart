{
  "name": "@view-server/react",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./testing": {
      "types": "./dist/testing.d.ts",
      "import": "./dist/testing.js"
    }
  },
  "scripts": {
    "bench:in-memory-live-query": "vp run -t @view-server/in-memory#build && sh -c 'rows=${VIEW_SERVER_REACT_BENCH_ROWS:-10000}; browser=${VIEW_SERVER_REACT_BENCH_BROWSER:-chromium}; output=${VIEW_SERVER_REACT_BENCH_OUTPUT_JSON:-.artifacts/in-memory-live-query-${rows}rows-${browser}.json}; mkdir -p .artifacts; VIEW_SERVER_REACT_SKIP_REMOTE_GLOBAL_SETUP=1 VITE_VIEW_SERVER_REACT_BENCH_ROWS=$rows VITE_VIEW_SERVER_REACT_BENCH_BATCH_SIZE=${VIEW_SERVER_REACT_BENCH_BATCH_SIZE:-1000} VITE_VIEW_SERVER_REACT_BENCH_ITERATIONS=${VIEW_SERVER_REACT_BENCH_ITERATIONS:-5} VITE_VIEW_SERVER_REACT_BENCH_TIME_MS=${VIEW_SERVER_REACT_BENCH_TIME_MS:-250} VITE_VIEW_SERVER_REACT_BENCH_WARMUP_ITERATIONS=${VIEW_SERVER_REACT_BENCH_WARMUP_ITERATIONS:-0} VITE_VIEW_SERVER_REACT_BENCH_WARMUP_TIME_MS=${VIEW_SERVER_REACT_BENCH_WARMUP_TIME_MS:-0} VITE_VIEW_SERVER_REACT_BENCH_OUTPUT_JSON=$output vp test bench src/in-memory-live-query.bench.tsx --run --testTimeout 0 --project $browser --outputJson $output'",
    "build": "vp run -t @view-server/effect-utils#build && vp run -t @view-server/in-memory#build && vp pack",
    "test": "vp run -t @view-server/effect-utils#build && vp run -t @view-server/in-memory#build && vp run -t @view-server/server#build && vp test run --coverage --typecheck"
  },
  "dependencies": {
    "@effect/atom-react": "catalog:",
    "@view-server/client": "workspace:*",
    "@view-server/config": "workspace:*",
    "@view-server/effect-utils": "workspace:*",
    "effect": "catalog:",
    "scheduler": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@types/react": "19.2.15",
    "@types/react-dom": "19.2.3",
    "@view-server/in-memory": "workspace:*",
    "@view-server/runtime-core": "workspace:*",
    "@view-server/server": "workspace:*",
    "@vitest/browser": "catalog:",
    "@vitest/browser-playwright": "catalog:",
    "@vitest/coverage-istanbul": "catalog:",
    "@vitest/runner": "catalog:",
    "playwright": "1.60.0",
    "react": "19.2.6",
    "react-dom": "19.2.6",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "vitest-browser-react": "catalog:"
  },
  "peerDependencies": {
    "react": "19.2.6",
    "react-dom": "19.2.6"
  }
}
